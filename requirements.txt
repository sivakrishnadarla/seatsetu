# College Growth OS — Module A (AI Admissions Copilot)
# Lean stack: pure Python, free-tier friendly, Vercel-deployable
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg[binary]>=3.1        # Postgres (Vercel Postgres / Neon free tier)
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.1
cryptography>=42.0
python-multipart>=0.0.9
openpyxl>=3.1                 # Accred: .xlsx marks upload parsing
reportlab>=4.0                # Brochure + guide PDF generation (pure-python)

# Production upgrades (documented in README):
#   langgraph, langfuse        -> orchestration + tracing
#   pgvector                   -> vector search at scale
#   whatsapp cloud api sdk     -> real WhatsApp sending
